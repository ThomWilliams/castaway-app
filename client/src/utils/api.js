// Function with paramters (these will be whatever details you get from the user to search for podcasts)

            // inside the function we make a await fetch request using the parameters which have been passed in

            // we get the response then and return it back


//export the function

export const getPodcastByCAtegory = (categoryId) => {
    return fetch(`........${categoryId}`m {
        headers: {},
        method, 
        .....
    })
}